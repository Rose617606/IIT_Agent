# LangGraph + LangChain 核心
langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0

# Embedding + Reranker（BGE-M3 + BGE-Reranker-v2-m3）
FlagEmbedding>=1.3.0

# 向量数据库（Supabase pgvector）
pgvector>=0.3.0
asyncpg>=0.30.0

# 前端原型
gradio>=4.0.0

# 结构化输出
pydantic>=2.0.0

# 环境变量管理
python-dotenv>=1.0.0

# YAML frontmatter 解析（知识库构建）
PyYAML>=6.0

# 测试
pytest>=8.0.0

# RAGAS 评测（Phase 5）
ragas>=0.2.0

# 记忆持久化（Phase 6）
# sqlite3 标准库自带，无需额外安装
